# API server
PORT=<API_PORT:3001>

# Comma-separated list of allowed origins in production
CORS_ORIGINS=https://<SWA_NAME>.azurestaticapps.net,https://<CUSTOM_DOMAIN>

# Add additional <ENV_VARS> here
